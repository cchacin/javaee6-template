<!--?xml version="1.0" encoding="UTF-8"?-->
<resources>
    <Resource id="jdbc/javaeeSampleDatabase" type="javax.sql.DataSource">
        # configure the pool
        DataSourceCreator = tomcat

        # it is a jta datasource
        JtaManaged = true

        # tomcat pool configuration
        driverClassName = com.mysql.jdbc.Driver
        url = jdbc:mysql://127.0.0.1:3306/javaee_sample
        username = root
        password =
        validationQuery = SELECT 1
    </Resource>
    <Resource id="jdbc/javaeeSampleDatabaseUnmanaged" type="javax.sql.DataSource">
        # configure the pool
        DataSourceCreator = tomcat

        # it is a jta datasource
        JtaManaged = false

        # tomcat pool configuration
        driverClassName = com.mysql.jdbc.Driver
        url = jdbc:mysql://127.0.0.1:3306/javaee_sample
        username = root
        password =
        validationQuery = SELECT 1
    </Resource>
</resources>
